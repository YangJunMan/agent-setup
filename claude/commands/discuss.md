---
description: Load the multi-model discussion rules before a cross-agent review
---

Read `DISCUSSION_RULES.md` — from the project root if present, otherwise
`~/.config/agent-setup/DISCUSSION_RULES.md` — and apply it to the request below only,
not to subsequent ordinary coding work. If it is empty, ask for
the `Goal`, `Constraints`, `Current Design`, `Evaluation Criteria`, and
`Open Questions` that the input contract requires.

$ARGUMENTS
